# 传奇服务端爆率模拟与修改工具 开发文档

> 最后更新：2026-09-18
> 项目地址：https://github.com/yueyoue/legend-drop-tool
> 当前版本：v2.0.4

---

## 1. 项目概述

### 1.1 项目背景

传奇私服运营、服务端调试过程中，怪物爆率失衡、掉落数据不透明、手动修改爆率繁琐等问题普遍存在。市面现有工具功能碎片化，部分工具模拟数据失真、修改兼容性差、无设备绑定授权机制，易出现盗版盗用、数据错乱等问题。

本工具聚焦传奇服务端专属场景，集成**高真实度爆率模拟**、**可视化爆率修改**、**设备绑定授权管理**三大核心能力，适配主流传奇服务端版本（HERO、GOM、GEE、BLUE），解决服务端爆率调试、优化、批量修改的核心痛点。

### 1.2 适配范围

- **适配服务端**：HERO、GOM、GEE、BLUE 主流传奇引擎
- **适配文件**：MonItems 目录下所有怪物爆率 .txt 文件
- **系统适配**：Windows 10/11（64位）

---

## 2. 技术选型

### 2.1 最终技术栈

| 项目 | 选型 | 说明 |
|------|------|------|
| 开发语言 | **Go 1.21** | 编译为原生二进制，反编译难度高，防破解能力强 |
| GUI框架 | **Wails v2** | WebView2 前端 + Go 后端绑定，开发效率高，UI 灵活 |
| 前端 | **原生 HTML/CSS/JS** | 无框架依赖，轻量直接 |
| 架构 | Go 后端绑定 + WebView2 渲染 | Go public 方法自动暴露为 `window.go.app.App.*` |
| CI/CD | **GitHub Actions** | 推送 tag 自动编译 exe 并发布 Release |
| 中文字体 | 系统字体 | WebView2 直接使用系统中文字体，无需内嵌 |

### 2.2 框架迁移记录（Fyne → Wails v2）

> v2.0.0 起由 Fyne v2 迁移至 Wails v2

| 对比项 | Fyne v2 | Wails v2 |
|--------|---------|----------|
| 渲染方式 | 自绘 Canvas | 系统 WebView2 |
| UI 开发 | Go 代码写控件 | HTML/CSS/JS 前端 |
| UI 灵活度 | 较低（原生控件） | **高**（完整 Web 能力） |
| 打包体积 | ~30MB（含 CGO） | ~11MB（纯 Go） |
| 编译速度 | 慢（CGO） | **快**（纯 Go） |
| 中文支持 | 需内嵌字体 | 系统字体直接可用 |
| 反编译难度 | 高 | 高 |
| 前后端通信 | 直接调用 | **绑定机制**（`Bind` + `window.go.app.App.*`） |

### 2.3 Wails v2 关键配置

```json
// wails.json
{
  "frontend:dir": "frontend/dist",
  "name": "legend-drop-tool",
  "outputfilename": "legend-drop-tool"
}
```

**后端绑定规则**：
- `main.go` 中 `Bind: []interface{}{application}` 将 `*App` 结构体暴露给前端
- Go 包名决定 JS 命名空间：`package app` → `window.go.app.App.*`
- 所有 `App` 的 public 方法自动绑定，前端通过 `await window.go.app.App.MethodName()` 调用
- 对话框（目录选择等）需要在 Go 侧通过 `runtime.OpenDirectoryDialog(ctx, ...)` 实现，前端调用 Go 绑定方法

**前端资源**：
- `frontend/dist/` 下的文件通过 `//go:embed all:frontend/dist` 嵌入二进制
- Wails 运行时自动注入 `/wails/runtime.js`（IPC + 绑定初始化）
- `window.go` 对象由运行时异步初始化，前端需确保绑定就绪后再调用

---

## 3. 爆率文件格式支持

### 3.1 文件路径

标准路径：`MirServer\Mir200\Envir\MonItems\`

每个怪物一个 `.txt` 文件，文件名为怪物名称（如 `白野猪.txt`）。

### 3.2 支持的格式清单

#### 传统格式（全部引擎）

```ini
; 注释行
1/100 裁决之杖           ; 1/100概率掉落裁决之杖
1/1 金币 10000           ; 必掉10000金币
1/200 圣战戒指 2          ; 1/200概率掉2个圣战戒指
```

#### GOM/GEE #CHILD 格式

```ini
; #CHILD+RANDOM：随机从组内抽取一件
#CHILD 1/1 RANDOM
(
1/1 天尊头盔
1/1 天尊项链
1/1 天尊手镯
1/1 天尊戒指
)

; #CHILD 子爆率：1/2概率进入组，按子爆率掉落
#CHILD 1/2
(
1/100 火球术
1/1 治愈术
)

; 嵌套#CHILD
#CHILD 1/1 RANDOM
(
1/1 天尊头盔
#CHILD 1/2
(
1/100 火球术
1/1 治愈术
)
)
```

#### GOM/GEE #CALL 引用

```ini
; 引用外部爆率文件
#CALL [\\爆率系统\\基础爆率.txt] @药水
```

#### GOM 精准爆率触发

```ini
; 物品名|@触发字段
1/10 开天|@爆开天触发检测
1/10 强效魔法药|@爆稀有装备检测
```

#### GOM #CASE/#IF 条件检测

```ini
; #CASE 变量检测
#CASE N10|1 RANDOM
100
(
1/1 太阳水|@爆稀有装备检测
1/1 木剑
)

; #IF 条件检测
#IF [N20 > 100, N20 < 110|1] RANDOM
(
1/1 井中月
1/1 炼狱
)
```

### 3.3 引擎自动检测

工具会通过以下方式自动检测引擎类型：
1. 检查服务端目录下的引擎控制器文件名
2. 通过爆率文件内容特征（#CHILD/#CALL/#CASE/#IF → GOM/GEE）
3. 检查 GlobalDropRate.txt 是否存在（HERO引擎特征）

---

## 4. 核心功能

### 4.1 爆率修改模块

| 功能 | 说明 | 状态 |
|------|------|------|
| 加载爆率文件 | 一键加载 MonItems 目录下所有 .txt 文件 | ✅ 已完成 |
| 文件列表展示 | 左侧树形列表显示怪物名称和条目数 | ✅ 已完成 |
| 条目详情展示 | 右侧列表结构化显示所有条目（区分类型/缩进） | ✅ 已完成 |
| 单条修改 | 双击条目弹窗修改概率/数量 | ✅ 已完成 |
| 新增条目 | 添加新的掉落配置 | ✅ 已完成 |
| 批量倍率调整 | 对所有可编辑条目统一乘以倍率 | ✅ 已完成 |
| 批量设置概率 | 对所有可编辑条目统一设置概率 | ✅ 已完成 |
| 爆率异常检测 | 检测超高爆率/极低爆率/零掉落 | ✅ 已完成 |
| 保存文件 | 修改后保存并自动备份原文件 | ✅ 已完成 |
| 备份管理 | 单文件备份/全目录备份/一键还原 | ✅ 已完成 |

### 4.2 爆率模拟模块

| 功能 | 说明 | 状态 |
|------|------|------|
| 模拟参数配置 | 时长/击杀比例/模拟轮数 | ✅ 已完成 |
| 单怪物模拟 | 对选中怪物运行掉落模拟 | ✅ 已完成 |
| 全部怪物模拟 | 对所有怪物运行掉落模拟 | ✅ 已完成 |
| 筛选模拟 | 指定物品/地图/怪物筛选，多选弹窗 | ✅ 已完成 |
| 物品点击联动 | 点击掉落物品后，地图和怪物列表自动筛选显示该物品的来源 | ✅ 已完成 |
| 地图点击联动 | 点击地图后，怪物列表自动筛选该地图的怪物 | ✅ 已完成 |
| 统计报表 | 总击杀/掉落/空爆率/物品维度统计 | ✅ 已完成 |
| 稀有物品追踪 | 自动追踪爆率 <0.1% 的物品 | ✅ 已完成 |
| 导出结果 | 导出模拟结果为文本文件 | ✅ 已完成 |

### 4.3 爆率调配模块

#### 4.3.1 功能概览

| 功能 | 说明 | 状态 |
|------|------|------|
| 物品掉落来源分析 | 分析指定物品被哪些怪物/地图掉落，计算每小时杀怪数和期望时间 | ✅ 已完成 |
| 模拟数据优先 | 已模拟时使用实际击杀数/掉落数/模拟时长计算，未模拟时回退到配置+MonGen 数据 | ✅ 已完成 |
| 物品搜索 | 物品下拉框支持关键词搜索，快速定位目标物品 | ✅ 已完成 |
| 数据来源提示 | 分析结果上方显示数据来源（模拟数据 / 配置文件+MonGen） | ✅ 已完成 |
| 目标时间设置 | 按地图设置目标多少小时出一个物品 | ✅ 已完成 |
| 推荐爆率计算 | 根据目标时间反算推荐爆率分母，向上取整到阶梯数字 | ✅ 已完成 |
| 推荐值可编辑 | 推荐爆率可在表格中手动调整，修改分母后期望小时实时重算更新 | ✅ 已完成 |
| 地图点击联动 | 模拟结果中点击地图行，怪物列表自动筛选该地图的怪物 | ✅ 已完成 |
| 一键应用推荐值 | 批量修改所有推荐的爆率条目（支持用户修改后的值） | ✅ 已完成 |
| 复制爆率到其他物品 | 将源物品的爆率配置复制到多个目标物品（多选），不存在则新增 | ✅ 已完成 |
| 一键备份 | 应用修改前全目录备份 | ✅ 已完成 |

#### 4.3.2 页面布局

```
┌─────────────────────────────────────────────────────────┐
│ [搜索物品...] [▼ 物品下拉选择]  [🔍 分析掉落来源]       │ ← 顶部操作区
├─────────────────────────────────────────────────────────┤
│ 📍 当前掉落来源 (N个来源)                               │
│ 📊 数据来源：模拟结果（击杀数/掉落数/模拟时长）          │ ← 数据来源提示
│ ┌────────┬────────┬──────┬────┬──────────┬──────────┐  │
│ │ 地图   │ 怪物   │ 爆率 │数量│每小时怪数│期望(h/个)│  │ ← 掉落来源表格
│ ├────────┼────────┼──────┼────┼──────────┼──────────┤  │    (max-height:320px
│ │ ...    │ ...    │1/3000│ 1  │   252.1  │   11.9   │  │     超出滚动)
│ └────────┴────────┴──────┴────┴──────────┴──────────┘  │
├─────────────────────────────────────────────────────────┤
│ 🎯 设置目标时间（小时/个）                              │
│ [祖玛寺庙: 15 h/个] [赤月峡谷: 20 h/个]                │ ← 每个地图一个卡片
│                [📊 计算推荐爆率]                        │
├─────────────────────────────────────────────────────────┤
│ 📊 推荐修改 (N条)                                       │
│ ┌────────┬────────┬────────┬──────────┬──────────┐    │
│ │ 地图   │ 怪物   │当前爆率│ 推荐爆率 │修改后期望│    │ ← 推荐表格
│ ├────────┼────────┼────────┼──────────┼──────────┤    │    (分母可编辑
│ │ ...    │ ...    │1/3000  │ 1/5000   │  19.8h   │    │     期望实时更新)
│ └────────┴────────┴────────┴──────────┴──────────┘    │
│ [💾 一键备份]  [✅ 应用推荐值]                          │
├─────────────────────────────────────────────────────────┤
│ 📋 复制爆率到其他物品                                   │
│ [▼ 目标物品多选列表]                                    │
│ [📋 应用到选中物品]                                     │
└─────────────────────────────────────────────────────────┘
```

#### 4.3.3 用户操作流程

```
1. 加载爆率文件（爆率修改页点击"加载爆率文件"）
2. （可选）运行爆率模拟获取更准确的数据
3. 切换到"爆率调配"标签页
4. 在物品下拉框中搜索并选择目标物品（如"裁决之杖"）
5. 点击"分析掉落来源"→ 显示该物品在哪些怪物/地图掉落
6. 检查数据来源提示（模拟数据 vs 配置数据）
7. 设置每个地图的目标时间（如祖玛寺庙 15小时/个）
8. 点击"计算推荐爆率"→ 显示推荐修改表
9. （可选）手动调整推荐爆率的分母值
10. 点击"应用推荐值"→ 修改爆率文件
11. （可选）复制爆率到其他物品
```

#### 4.3.4 数据流与函数调用链

```
前端 (main.js)                         后端 (internal/app/app.go)
─────────────────                      ─────────────────────────
点击"分析掉落来源"
  │
  ├─► AnalyzeItemDrops(itemName)
  │     │
  │     ├─ simResult != nil ?
  │     │   ├─ YES → analyzeFromSim(itemName)
  │     │   │         遍历 simResult.ItemMonsterDrops[itemName]
  │     │   │         获取 killCnt(模拟击杀), simDropCnt(模拟掉落)
  │     │   │         kph = killCnt / durationH
  │     │   │         actualProb = simDropCnt / killCnt
  │     │   │         expectH = 1 / (kph * actualProb)
  │     │   │
  │     │   └─ NO  → 遍历 currentResults 配置文件
  │     │             kph = MonGen count * 3600 / refreshSec
  │     │             prob = entry.Probability()
  │     │             expectH = 1 / (kph * prob)
  │     │
  │     └─► 返回 RateAnalysis { sources[], totalKph, totalExpectH }
  │
  ├─► renderTuneSources(hasSim)  → 渲染掉落来源表格 + 数据来源提示
  ├─► renderTuneTargets()        → 按地图去重，生成目标时间输入卡片
  │
点击"计算推荐爆率"
  │
  ├─► RecommendRates(itemName, targets[])
  │     │
  │     │  对每个掉落条目：
  │     ├─► calcExpectHours(monsterName, num, den)
  │     │     ├─ simResult != nil ? 用模拟数据 : 用 MonGen
  │     │     └─ 返回 oldExpectH（当前期望小时）
  │     │
  │     ├─ rawDen = oldDen * targetH / oldExpectH
  │     ├─ newDen = roundToNiceDenominator(rawDen)  // 向上取整
  │     ├─ newExpectH = oldExpectH * newDen / oldDen
  │     │
  │     └─► 返回 RateChange[] { oldDen, oldExpectH, newDen, newExpectH }
  │
  ├─► renderTuneRecommend()  → 渲染推荐表格
  │     │
  │     └─ 每行 <tr> 带 data-old-exp 和 data-old-den
  │        分母 input 的 oninput 事件：
  │        newExpectH = oldExpectH * newDen / oldDen
  │
点击"应用推荐值"
  │
  ├─ 从表格读取用户修改后的 newDen
  ├─► ApplyRecommendedRates(itemName, changes[])
  │     遍历 changes → editor.ModifyEntry(file, entry, newNum, newDen)
  │
  └─► SaveAllModifiedFiles()  → 保存所有修改过的文件
```

#### 4.3.5 核心公式详解

**数据来源优先级**：
- 优先使用模拟数据（`simResult != nil` 时）：击杀数、掉落数、模拟时长均来自模拟结果
- 回退到配置数据（未模拟时）：使用 MonGen 刷新信息 + 爆率文件概率
- `calcExpectHours()` 函数封装了这个优先级逻辑，分析和推荐共用

**当前期望小时**：
```
模拟数据：kph = 模拟击杀数 / 模拟时长(h)
          actualProb = 模拟掉落数 / 模拟击杀数
          expectH = 1 / (kph * actualProb)

配置数据：kph = MonGen每次数量 * 3600 / 刷新间隔秒数
          prob = 爆率分子 / 爆率分母
          expectH = 1 / (kph * prob)
```

**推荐分母**：
```
newDen = oldDen * targetH / oldExpectH
```
- `oldDen`：当前爆率分母（如 3000）
- `targetH`：用户设置的目标小时（如 15）
- `oldExpectH`：当前期望小时（如 11.9）
- 计算结果向上取整到阶梯数字（1/2/5/10/20/50/100/200/500/1000...）
- **不依赖 KPH**，直接用期望时间反算，确保分析和推荐数据一致

**修改后期望**：
```
newExpectH = oldExpectH * newDen / oldDen
```
- 期望与分母成正比：分母越大 → 爆率越低 → 期望越长
- 前端实时更新使用同一公式

**计算示例**：
```
当前：爆率 1/3000，期望 11.9 小时/个
目标：15 小时/个

推荐分母 = 3000 * 15 / 11.9 = 3782 → 向上取整到 5000
推荐爆率 = 1/5000
修改后期望 = 11.9 * 5000 / 3000 = 19.8 小时/个

说明：目标 15h，但阶梯取整到 5000 后实际 19.8h
     如果取 4000 则 15.9h 更接近，但 4000 不是"好看"数字
```

**阶梯取整规则**（`roundToNiceDenominator`）：
```
输入 raw → 向上找到第一个 >= raw 的阶梯值
阶梯：1, 2, 5, 10, 20, 50, 100, 200, 500, 1000, 2000, 5000, 10000...
超过 1000000 → 向上取整到万的倍数

示例：3782 → 5000 | 120 → 200 | 85 → 100 | 1050 → 2000
```

#### 4.3.6 前端关键状态

```javascript
tuneAnalysis   // RateAnalysis 对象，分析结果（含 sources[]）
tuneRecommend  // RateChange[] 数组，推荐修改列表
```

**实时更新机制**：
- 推荐表格每行 `<tr>` 带 `data-old-exp`（当前期望）和 `data-old-den`（当前分母）
- 分母 `<input>` 的 `oninput` 事件触发时：`newExpectH = oldExpectH * newDen / oldDen`
- 同步更新 `tuneRecommend[idx].newDen` 和 `tuneRecommend[idx].newExpectH`
- 点击"应用推荐值"时从表格读取最新的 newDen 值

#### 4.3.7 常见问题排查指南

| 现象 | 可能原因 | 排查方法 |
|------|----------|----------|
| 分析结果期望时间不准 | 未运行模拟，使用了配置数据 | 检查状态栏是否显示"配置文件+MonGen数据"，先运行模拟 |
| 推荐爆率方向反了（应变大却变小） | `roundToNiceDenominator` 向下取整 | 检查该函数是否用 `>= raw` 而非 `>= raw*0.8` |
| 推荐后期望时间与目标差距大 | 阶梯取整的精度限制 | 正常现象，阶梯数字离散导致；可手动调整分母 |
| 分析和推荐的期望时间不一致 | 两者用了不同的 KPH 数据源 | 检查 `RecommendRates` 是否调用 `calcExpectHours` |
| 修改分母后期望不更新 | `oninput` 事件未绑定 | 检查 `renderTuneRecommend` 末尾的事件绑定代码 |
| 表格列不对齐 | colgroup 宽度不一致或表格容器有滚动 | 检查两个表格的 `<colgroup>` 固定像素宽度是否一致 |
| 表格撑长页面 | 缺少 max-height | 检查 `.tune-table-wrap` 的 `max-height: 320px` |
| 应用推荐值后文件没变 | `ApplyRecommendedRates` 的 monsterIndex/entryIndex 错误 | 检查 RateChange 中的索引是否与 currentResults 对应 |
| 复制爆率到其他物品无效 | 目标物品在源怪物中不存在 | 检查 `CopyRatesToItems` 是否处理了新增条目的情况 |

### 4.4 授权管理模块

| 功能 | 说明 | 状态 |
|------|------|------|
| 机器码获取 | 采集CPU/主板/硬盘信息生成唯一机器码 | ✅ 已完成 |
| 激活码验证 | 验证激活码并绑定设备 | ✅ 已完成 |
| 授权状态检查 | 启动时自动校验授权 | ✅ 已完成 |
| 权限分级 | 试用/永久/季度授权 | 🔲 接口预留 |

---

## 5. 项目结构

```
legend-drop-tool/
├── main.go                        # Wails 入口
├── wails.json                     # Wails 项目配置
├── go.mod                         # Go 模块定义
├── internal/
│   └── app/
│       └── app.go                 # 后端绑定（所有 public 方法自动暴露给前端）
├── frontend/
│   └── dist/                      # 前端静态资源（//go:embed 嵌入二进制）
│       ├── index.html             # 主页面
│       ├── main.js                # 前端逻辑（调用 window.go.app.App.*）
│       └── style.css              # 样式（暗色主题）
├── pkg/
│   ├── parser/                    # 爆率文件解析
│   │   ├── types.go               # 数据类型定义
│   │   └── parser.go              # 解析引擎（支持全部格式）
│   ├── editor/                    # 爆率编辑
│   │   └── editor.go              # 编辑器（修改/新增/批量/重建）
│   ├── simulator/                 # 爆率模拟
│   │   └── simulator.go           # 模拟引擎
│   ├── backup/                    # 备份管理
│   │   └── backup.go              # 备份/恢复
│   ├── auth/                      # 授权管理
│   │   └── auth.go                # 机器码 + 激活码
│   └── config/                    # 配置管理
│       └── config.go              # 应用配置持久化
├── fonts/
│   └── fonts.go                   # 字体嵌入（预留）
├── .github/workflows/
│   └── build.yml                  # CI/CD 自动编译
├── README.md
└── 开发文档.md                      # 本文档
```

---

## 6. 开发进度

### 一期（基础完整版）— ✅ 已完成

- [x] 搭建 Go + Fyne 项目框架
- [x] 实现爆率文件解析引擎（HERO/GOM/GEE/BLUE）
- [x] 支持全部主流格式（传统/#CHILD/#CALL/#CASE/#IF/精准触发）
- [x] 实现爆率修改功能（单条/批量/倍率/新增/删除）
- [x] 实现爆率模拟功能（真实掉落逻辑）
- [x] 实现自动备份管理
- [x] 实现 GUI 界面（4个标签页）
- [x] 配置 GitHub Actions 自动编译 exe
- [x] 解决中文乱码问题（CJK主题）
- [x] 授权接口预留（骨架代码）

### 二期（Wails 迁移 + 功能增强）— ✅ 已完成

- [x] **框架迁移**：Fyne v2 → Wails v2（WebView2 + Go 绑定）
- [x] 前端重构：原生 HTML/CSS/JS 暗色主题界面
- [x] 后端绑定：Go public 方法自动暴露为 `window.go.app.App.*`
- [x] 目录选择对话框：通过 `runtime.OpenDirectoryDialog` Go 绑定实现
- [x] 模拟筛选功能：指定物品/地图/怪物多选弹窗（带搜索、全选）
- [x] 筛选数据传递到模拟请求

### 三期（爆率调配）— ✅ 已完成

- [x] 爆率调配新页面（🎯 爆率调配 tab）
- [x] 指定物品掉落来源分析（哪些怪物/地图掉落、期望时间）
- [x] 模拟数据优先：已模拟时使用实际数据，未模拟时回退配置数据
- [x] 按目标时间反算推荐爆率
- [x] 推荐爆率可编辑 + 修改后期望实时更新 + 一键应用
- [x] 爆率复制到其他物品（多选）
- [x] 修改前一键备份
- [x] 模拟结果物品点击联动（点击物品→筛选地图和怪物）
- [x] 模拟结果地图点击联动（点击地图→筛选怪物）
- [x] 移除模拟保底选项
- [x] 物品搜索过滤（爆率调配下拉框支持搜索）
- [x] 数据来源提示（区分模拟数据/配置数据）
- [x] 调配表格高度限制（max-height + 滚动，防止页面被撑长）

### 四期（授权与增值）— 计划中

- [ ] 实现云端授权后台
- [ ] 权限分级管理
- [ ] 软件自动更新功能

---

## 7. 编译与发布

### 本地编译

```bash
# 安装 Wails CLI
go install github.com/wailsapp/wails/v2/cmd/wails@latest

# 编译（Windows）
wails build -platform windows/amd64 -o legend-drop-tool.exe -ldflags "-s -w"
```

### 自动编译（GitHub Actions）

推送 tag 自动触发编译并发布 Release：

```bash
git tag v2.0.4
git push origin v2.0.4
```

编译产物：`legend-drop-tool.exe`（单文件，免安装，约11MB）

---

## 8. 前后端通信规范

### 8.1 绑定调用约定

```javascript
// 前端调用 Go 方法的标准写法
const result = await window.go.app.App.MethodName(arg1, arg2);

// 注意：
// - 命名空间是 window.go.app.App（不是 window.go.main.App）
// - Go 包名 package app 决定了 'app' 这一级
// - 所有 public 方法自动绑定
```

### 8.2 对话框调用

```go
// Go 侧：通过 Wails runtime 打开系统对话框
import "github.com/wailsapp/wails/v2/pkg/runtime"

func (a *App) SelectDirectory() (string, error) {
    return runtime.OpenDirectoryDialog(a.ctx, runtime.OpenDialogOptions{
        Title: "选择目录",
    })
}
```

```javascript
// 前端：调用 Go 绑定方法（不要用 window.runtime.OpenDirectoryDialog）
const path = await window.go.app.App.SelectDirectory();
```

### 8.3 初始化时序

```javascript
// window.go 由 Wails 运行时异步初始化，需等待就绪
async function waitForWailsReady(maxWaitMs = 5000) {
  return new Promise((resolve, reject) => {
    if (window.go && window.go.app && window.go.app.App) return resolve();
    const start = Date.now();
    const timer = setInterval(() => {
      if (window.go && window.go.app && window.go.app.App) {
        clearInterval(timer);
        resolve();
      } else if (Date.now() - start > maxWaitMs) {
        clearInterval(timer);
        reject(new Error('Wails 绑定加载超时'));
      }
    }, 50);
  });
}

window.addEventListener('DOMContentLoaded', async () => {
  await waitForWailsReady();
  // 初始化应用...
});
```

### 8.4 爆率调配 API

```javascript
// 1. 分析物品掉落来源（优先使用模拟数据）
const analysis = await window.go.app.App.AnalyzeItemDrops('裁决之杖');
// 返回: { itemName, sources: [{ monsterName, monsterIndex, entryIndex, mapName, probStr, killPerHour, expectHours }], totalKph, totalExpectH }
// 说明: 如果已运行模拟，killPerHour 基于实际模拟击杀数/时长，expectHours 基于实际掉率
//       如果未模拟，使用 MonGen 刷新信息 + 爆率文件概率计算，并在状态栏提示

// 2. 计算推荐爆率
const targets = [
  { mapName: '祖玛寺庙', targetHours: 7 },
  { mapName: '赤月峡谷', targetHours: 10 }
];
const changes = await window.go.app.App.RecommendRates('裁决之杖', targets);
// 返回: [{ monsterName, monsterIndex, entryIndex, mapName, oldNum, oldDen, oldExpectH, newNum, newDen, newExpectH }]

// 3. 应用推荐爆率（支持前端修改 newDen 后传入）
const count = await window.go.app.App.ApplyRecommendedRates('裁决之杖', changes);
// 前端修改 newDen 后，changes 中的 newDen 会被更新后的值覆盖

// 4. 复制爆率到其他物品
const result = await window.go.app.App.CopyRatesToItems('裁决之杖', ['骨玉权杖', '龙纹剑']);
// 返回: { success, modified, message }

// 5. 保存所有已修改文件
const saved = await window.go.app.App.SaveAllModifiedFiles();
```

**前端实时更新逻辑**：
```javascript
// 推荐表格中修改分母后，期望小时实时重算
// 每行 tr 带有 data-old-exp（当前期望小时）和 data-old-den（当前分母）
// oninput 事件触发时：newExpectH = oldExpectH * newDen / oldDen
// 同步更新 tuneRecommend 数组数据，确保应用时使用修改后的值
```

---

## 9. 已知问题与解决方案

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `window.runtime.OpenDirectoryDialog is not a function` | Wails v2 前端 runtime 没有此方法 | 改用 Go 绑定 `window.go.app.App.SelectDirectory()` |
| `undefined: runtime.DialogOptions` | Wails v2.16.0 类型名为 `OpenDialogOptions` | 使用 `runtime.OpenDialogOptions` |
| `Cannot read properties of undefined (reading 'App')` | JS 命名空间错误 | Go 包名是 `app`，应用 `window.go.app.App.*` 而非 `window.go.main.App.*` |
| 爆率文件编码 | 部分文件为 ANSI 编码 | 解析器已支持 BOM 检测和自动处理 |
| 爆率调配推荐值错误 | 推荐分母向下取整 + 分析/推荐用不同 KPH | 已修复：公式改为 `newDen = oldDen × targetH / oldExpectH`，不依赖 KPH，向上取整 |
| 物品点击无反应 | 点击物品行未绑定事件 | 已修复：点击物品后自动筛选地图和怪物列表 |
| 地图点击无反应 | 点击地图行未绑定事件 | 已修复：点击地图后自动筛选该地图的怪物列表 |
| 调配表格列不对齐 | 表格使用百分比宽度，不同表格列宽不一致 | 已修复：改用固定像素宽度 colgroup + width:max-content |

---

## 10. 参考工具

- [GM工具箱-爆率调整工具](https://mon.gmgjx.net/) — 功能参考
- [64GM论坛-爆率格式说明](https://www.64gm.com/thread-25391-1-1.html) — 格式参考
- [GM爱好者-GOM爆率教程](https://www.gmahz.com/thread-9988-1-1.html) — 格式参考
- [Wails v2 官方文档](https://wails.io/docs/) — 框架文档