# 传奇服务端爆率模拟与修改工具 开发文档（爆率调配修正版）

最后更新：2026-09-19
项目地址：[https://github.com/yueyoue/legend-drop-tool](https://github.com/yueyoue/legend-drop-tool)
当前版本：v2.3.0（地图名显示 + 复制爆率 + 多地图怪物高亮）

---

## 1. 项目概述

### 1.1 项目背景

传奇私服运营、服务端调试过程中，怪物爆率失衡、掉落数据不透明、手动修改爆率繁琐等问题普遍存在。市面现有工具功能碎片化，**部分工具爆率调配计算逻辑存在方向性错误**，导致修改后爆率与预期完全相反；同时部分模拟数据失真、修改兼容性差、无设备绑定授权机制，易出现盗版盗用、数据错乱等问题。

本工具聚焦传奇服务端专属场景，集成高真实度爆率模拟、可视化爆率修改、精准爆率调配、设备绑定授权管理四大核心能力，适配主流传奇服务端版本（HERO、GOM、GEE、BLUE），解决服务端爆率调试、优化、批量修改的核心痛点。**本版本重点重构爆率调配模块数学模型，修正原计算方向错误，优化取整策略，大幅提升推荐准确率**。

### 1.2 适配范围

- 适配服务端：HERO、GOM、GEE、BLUE 主流传奇引擎
- 适配文件：MonItems 目录下所有怪物爆率 .txt 文件
- 系统适配：Windows 10/11（64 位）

---

## 2. 技术选型

### 2.1 最终技术栈

表格

| 项目 | 选型 | 说明 |
| --- | --- | --- |
| 开发语言 | Go 1.21 | 编译为原生二进制，反编译难度高，防破解能力强 |
| GUI 框架 | Wails v2 | WebView2 前端 + Go 后端绑定，开发效率高，UI 灵活 |
| 前端 | 原生 HTML/CSS/JS | 无框架依赖，轻量直接 |
| 架构 | Go 后端绑定 + WebView2 渲染 | Go public 方法自动暴露为 `window.go.app.App.*` |
| CI/CD | GitHub Actions | 推送 tag 自动编译 exe 并发布 Release |
| 中文字体 | 系统字体 | WebView2 直接使用系统中文字体，无需内嵌 |

### 2.2 框架迁移记录（Fyne → Wails v2）

v2.0.0 起由 Fyne v2 迁移至 Wails v2

表格

| 对比项 | Fyne v2 | Wails v2 |
| --- | --- | --- |
| 渲染方式 | 自绘 Canvas | 系统 WebView2 |
| UI 开发 | Go 代码写控件 | HTML/CSS/JS 前端 |
| UI 灵活度 | 较低（原生控件） | 高（完整 Web 能力） |
| 打包体积 | ~30MB（含 CGO） | ~11MB（纯 Go） |
| 编译速度 | 慢（CGO） | 快（纯 Go） |
| 中文支持 | 需内嵌字体 | 系统字体直接可用 |
| 反编译难度 | 高 | 高 |
| 前后端通信 | 直接调用 | 绑定机制（`Bind` + `window.go.app.App.*`） |

### 2.3 Wails v2 关键配置

```
// wails.json
{
  "frontend:dir": "frontend/dist",
  "name": "legend-drop-tool",
  "outputfilename": "legend-drop-tool"
}
```

后端绑定规则：

- `main.go` 中 `Bind: []interface{}{application}` 将 `*App` 结构体暴露给前端
- Go 包名决定 JS 命名空间：`package app` → `window.go.app.App.*`
- 所有 `App` 的 public 方法自动绑定，前端通过 `await window.go.app.App.MethodName()` 调用
- 对话框（目录选择等）需要在 Go 侧通过 `runtime.OpenDirectoryDialog(ctx, ...)` 实现，前端调用 Go 绑定方法

前端资源：

- `frontend/dist/` 下的文件通过 `//go:embed all:frontend/dist` 嵌入二进制
- Wails 运行时自动注入 `/wails/runtime.js` （IPC + 绑定初始化）
- `window.go` 对象由运行时异步初始化，前端需确保绑定就绪后再调用

---

## 3. 爆率文件格式支持

### 3.1 文件路径

标准路径： `MirServer\Mir200\Envir\MonItems\`
每个怪物一个 `.txt` 文件，文件名为怪物名称（如 `白野猪.txt`）。

### 3.2 支持的格式清单

#### 传统格式（全部引擎）

```
; 注释行
1/100 裁决之杖           ; 1/100概率掉落裁决之杖
1/1 金币 10000           ; 必掉10000金币
1/200 圣战戒指 2          ; 1/200概率掉2个圣战戒指
```

#### GOM/GEE #CHILD 格式

```
; #CHILD+RANDOM：随机从组内抽取一件
#CHILD 1/1 RANDOM
(
1/1 天尊头盔
1/1 天尊项链
1/1 天尊手镯
1/1 天尊戒指
)

; #CHILD 子爆率：1/2概率进入组，按子爆率掉落
#CHILD 1/2
(
1/100 火球术
1/1 治愈术
)

; 嵌套#CHILD
#CHILD 1/1 RANDOM
(
1/1 天尊头盔
#CHILD 1/2
(
1/100 火球术
1/1 治愈术
)
)
```

#### GOM/GEE #CALL 引用

```
; 引用外部爆率文件
#CALL [\\爆率系统\\基础爆率.txt] @药水
```

#### GOM 精准爆率触发

```
; 物品名|@触发字段
1/10 开天|@爆开天触发检测
1/10 强效魔法药|@爆稀有装备检测
```

#### GOM #CASE/#IF 条件检测

```
; #CASE 变量检测
#CASE N10|1 RANDOM
100
(
1/1 太阳水|@爆稀有装备检测
1/1 木剑
)

; #IF 条件检测
#IF [N20 > 100, N20 < 110|1] RANDOM
(
1/1 井中月
1/1 炼狱
)
```

### 3.3 引擎自动检测

工具会通过以下方式自动检测引擎类型：

1. 检查服务端目录下的引擎控制器文件名
2. 通过爆率文件内容特征（#CHILD/#CALL/#CASE/#IF → GOM/GEE）
3. 检查 GlobalDropRate.txt 是否存在（HERO 引擎特征）

---

## 4. 核心功能

### 4.1 爆率修改模块

表格

| 功能 | 说明 | 状态 |
| --- | --- | --- |
| 加载爆率文件 | 一键加载 MonItems 目录下所有 .txt 文件 | ✅ 已完成 |
| 文件列表展示 | 左侧树形列表显示怪物名称和条目数 | ✅ 已完成 |
| 条目详情展示 | 右侧列表结构化显示所有条目（区分类型 / 缩进） | ✅ 已完成 |
| 单条修改 | 双击条目弹窗修改概率 / 数量 | ✅ 已完成 |
| 新增条目 | 添加新的掉落配置 | ✅ 已完成 |
| 批量倍率调整 | 对所有可编辑条目统一乘以倍率 | ✅ 已完成 |
| 批量设置概率 | 对所有可编辑条目统一设置概率 | ✅ 已完成 |
| 爆率异常检测 | 检测超高爆率 / 极低爆率 / 零掉落 | ✅ 已完成 |
| 保存文件 | 修改后保存并自动备份原文件 | ✅ 已完成 |
| 备份管理 | 单文件备份 / 全目录备份 / 一键还原 | ✅ 已完成 |
| 地图视图模式 | 按地图查看怪物和爆率，三列级联布局（地图→怪物→物品） | ✅ v2.2.0 |

#### 4.1.1 地图视图模式（v2.2.0 新增）

##### 功能说明

爆率修改页新增「地图视图」模式，支持按地图维度查看怪物及其掉落配置，解决传统怪物列表模式下无法直观了解“哪个地图有哪些怪物、每个怪物掉什么”的痛点。

##### 操作方式

编辑页工具栏新增两个切换标签：
- **👹 怪物列表** — 原有的左侧怪物列表模式（默认）
- **🗺️ 地图视图** — 新增的三列级联布局

##### 三列级联布局

```
┌─────────────────────────────────────────────────────────────────────┐
│ [👹 怪物列表] [🗺️ 地图视图]  [+ 新增] [x 批量倍率] ... [保存]    │ ← 工具栏
├───────────────┬───────────────┬─────────────────────────────────────┤
│ 🗺️ 地图列表  │ 👹 怪物列表   │ 📋 掉落物品                         │
│ (240px)       │ (240px)       │ (自适应剩余宽度)                    │
├───────────────┼───────────────┼─────────────────────────────────────┤
│ [搜索地图...] │ [搜索怪物...] │ 1/100 裁决之杖 x1                   │
│               │               │ 1/1(必掉) 金币 x10000               │
│ D001 (银杏谷) │ 半兽人 3条    │ 1/200 圣战戒指 x1                   │
│ D002 (比奇)   │ 骷髅战士 5条 │ #CHILD 1/2                          │
│ D003 (盟重)   │ 白野猪 8条   │ (                                   │
│ ...           │ ...          │   1/100 火球术                      │
│               │               │   1/1 治愈术                        │
│               │               │ )                                   │
└───────────────┴───────────────┴─────────────────────────────────────┘
```

##### 级联行为

| 操作 | 左列（地图） | 中列（怪物） | 右列（物品） |
| --- | --- | --- | --- |
| 初始加载 | 显示所有地图（来自 MonGen.txt） | 清空 | 清空 |
| 点击地图 | 高亮选中 | 显示该地图下所有怪物（怪物名+掉落条目数） | 清空 |
| 点击怪物 | 保持不变 | 高亮选中 | 显示该怪物全部掉落条目（可编辑） |
| 点击物品 | — | — | 弹出修改对话框（复用原有编辑弹窗） |

##### 数据来源与关联逻辑

```
MonGen.txt                    MapInfo.txt
(地图号 X Y 怪物名 ...)      (地图号 地图描述)
     │                              │
     ▼                              ▼
  地图→怪物映射               地图号→中文名
     │                              │
     └──────────┬───────────────────┘
                ▼
         GetMapMonsterView()
         返回: [{mapName, displayName, monsterCount}]
                │
                ▼
         GetMapMonsters(mapName)
         遍历 currentResults 匹配怪物名
         返回: [{monsterName, monsterIndex, entryCount}]
                │
                ▼
         GetEntries(monsterIndex)  ← 已有 API
         返回该怪物的全部掉落条目
```

- **地图名**：来自 MonGen.txt 的地图号字段，自动关联 MapInfo.txt 显示中文描述（如 `D001 (银杏山谷)`）
- **怪物→地图**：通过 MonGen.txt 建立映射关系
- **怪物→爆率文件**：怪物名匹配 currentResults 中的 MonsterDropFile
- **降级处理**：若 MonGen.txt 无数据，从怪物掉落文件名推断地图关系

##### UI 行为

- 进入地图视图时：隐藏左侧怪物列表 sidebar，隐藏编辑操作按钮（新增/批量/备份/保存）
- 退出地图视图时：恢复 sidebar 和编辑按钮
- 三列均支持搜索过滤（输入关键词实时筛选）
- 掉落物品列表跳过纯注释行，只显示实际掉落条目
- 点击可编辑条目弹出修改对话框，保存后自动刷新当前列表
- **复制爆率**：怪物列表支持 checkbox 多选，右键菜单可复制当前怪物爆率并粘贴到选中怪物
- **多地图怪物高亮**：出现在多个地图的怪物以蓝色高亮+左边框显示，鼠标悬浮提示其他出现地图
- 模拟爆率掉落地图列表显示地图中文名（关联 MapInfo.txt）

### 4.2 爆率模拟模块

#### 功能清单

表格

| 功能 | 说明 | 状态 |
| --- | --- | --- |
| 模拟参数配置 | 时长 / 击杀比例 / 模拟轮数 / 人物爆率 | ✅ 已完成 |
| 单怪物模拟 | 对选中怪物运行掉落模拟 | ✅ 已完成 |
| 全部怪物模拟 | 对所有怪物运行掉落模拟 | ✅ 已完成 |
| 筛选模拟 | 指定物品 / 地图 / 怪物筛选，多选弹窗 | ✅ 已完成 |
| 物品点击联动 | 点击掉落物品后，地图和怪物列表自动筛选显示该物品的来源 | ✅ 已完成 |
| 地图点击联动 | 点击地图后，怪物列表自动筛选该地图的怪物 | ✅ 已完成 |
| 统计报表 | 总击杀 / 掉落 / 空爆率 / 物品维度统计 | ✅ 已完成 |
| 稀有物品追踪 | 自动追踪爆率 <0.1% 的物品 | ✅ 已完成 |
| 导出结果 | 导出模拟结果为文本文件 | ✅ 已完成 |

#### 核心模拟模型（内置真实击杀速率限制）

模拟引擎严格遵循传奇真实刷怪逻辑，**玩家击杀速度受怪物刷新上限约束**，避免理论值失真：

1. **玩家理论击杀速度**：`kill_speed = 挂机效率系数 / 单只怪物击杀耗时`（单位：只 / 秒）
2. **怪物刷新速度**：`spawn_speed = 地图常驻怪物数量 / 怪物刷新间隔`（单位：只 / 秒）
3. **真实击杀速度**：`real_kps = min(kill_speed, spawn_speed)`（取两者最小值，怪刷不出来时无法无限击杀）
4. 每次击杀执行独立随机判定，命中则记录掉落事件（怪物、时间、物品）
5. 支持多轮蒙特卡洛模拟，输出均值、分布区间与掉落明细

> 
> 该模型确保模拟结果接近真实挂机场景，模拟产出的击杀数、掉落数可直接用于爆率调配计算。

### 4.3 爆率调配模块（核心重构・修正版）

#### 4.3.1 原方案问题根因分析

v2.0.x 版本爆率调配存在两类核心问题，导致推荐结果准确率低、甚至方向完全相反：

表格

| 问题 | 现象 | 根因 |
| --- | --- | --- |
| 计算方向错误 | 目标 5 小时出 1 件，推荐后反而 0.2 小时出 1 件 | 爆率分母与期望时间的正比关系搞反：目标时间变长，分母反而调小，爆率变高 |
| 阶梯取整粗糙 | 目标 15 小时，取整后实际 19.8 小时，偏差 32% | 阶梯跨度过大（3782 直接跳到 5000），且未做取整后反向校验 |
| 理论值失真 | 配置计算的期望时间与模拟结果差距大 | 配置数据下的 kph 未考虑怪物刷新上限，默认玩家能无限杀怪 |
| 无偏差提示 | 用户不知道推荐值与目标的实际差距 | 取整后未重新计算真实期望并展示偏差 |

本版本完全重构底层数学模型，统一模拟与配置的计算逻辑，优化取整策略，新增偏差校验。

#### 4.3.2 功能概览

表格

| 功能 | 说明 | 状态 |
| --- | --- | --- |
| 物品掉落来源分析 | 分析指定物品被哪些怪物 / 地图掉落，计算每小时杀怪数和期望时间 | ✅ 重构完成 |
| 模拟数据优先 | 已模拟时使用实际击杀数 / 掉落数 / 模拟时长计算，未模拟时回退到配置 + MonGen 数据 | ✅ 保留优化 |
| 物品搜索 | 物品下拉框支持关键词搜索，快速定位目标物品 | ✅ 已完成 |
| 数据来源提示 | 分析结果上方显示数据来源（模拟数据 / 配置文件 + MonGen） | ✅ 已完成 |
| 目标时间设置 | 按地图设置目标多少小时出一个物品 | ✅ 已完成 |
| 推荐爆率计算 | 根据目标时间反算推荐爆率分母，支持多种取整模式 | ✅ 重构完成 |
| 推荐值可编辑 | 推荐爆率可在表格中手动调整，修改分母后期望小时实时重算更新 | ✅ 已完成 |
| 偏差展示 | 每条推荐显示与目标的偏差百分比 | ✅ 新增 |
| 地图点击联动 | 模拟结果中点击地图行，怪物列表自动筛选该地图的怪物 | ✅ 已完成 |
| 一键应用推荐值 | 批量修改所有推荐的爆率条目（支持用户修改后的值） | ✅ 已完成 |
| 复制爆率到其他物品 | 将源物品的爆率配置复制到多个目标物品（多选），不存在则新增 | ✅ 已完成 |
| 一键备份 | 应用修改前全目录备份 | ✅ 已完成 |

#### 4.3.3 页面布局

```
┌─────────────────────────────────────────────────────────┐
│ [搜索物品...] [▼ 物品下拉选择]  [🔍 分析掉落来源]       │ ← 顶部操作区
├─────────────────────────────────────────────────────────┤
│ 📍 当前掉落来源 (N个来源)                               │
│ 📊 数据来源：模拟结果（击杀数/掉落数/模拟时长）          │ ← 数据来源提示
│ ┌────────┬────────┬──────┬────┬──────────┬──────────┐  │
│ │ 地图   │ 怪物   │ 爆率 │数量│每小时怪数│期望(h/个)│  │ ← 掉落来源表格
│ ├────────┼────────┼──────┼────┼──────────┼──────────┤  │    (max-height:320px
│ │ ...    │ ...    │1/3000│ 1  │   252.1  │   11.9   │  │     超出滚动)
│ └────────┴────────┴──────┴────┴──────────┴──────────┘  │
├─────────────────────────────────────────────────────────┤
│ 🎯 设置目标时间（小时/个）                              │
│ [祖玛寺庙: 15 h/个] [赤月峡谷: 20 h/个]                │ ← 每个地图一个卡片
│ [取整模式：▼ 精准取整]  [📊 计算推荐爆率]              │ ← 新增取整模式选择
├─────────────────────────────────────────────────────────┤
│ 📊 推荐修改 (N条)                                       │
│ ┌────────┬────────┬────────┬──────────┬──────────┬───────┐ │
│ │ 地图   │ 怪物   │当前爆率│ 推荐爆率 │修改后期望│ 偏差  │ │ ← 新增偏差列
│ ├────────┼────────┼────────┼──────────┼──────────┼───────┤ │    (分母可编辑
│ │ ...    │ ...    │1/3000  │ 1/3782   │  15.0h   │ +0%  │ │     期望实时更新)
│ └────────┴────────┴────────┴──────────┴──────────┴───────┘ │
│ [💾 一键备份]  [✅ 应用推荐值]                          │
├─────────────────────────────────────────────────────────┤
│ 📋 复制爆率到其他物品                                   │
│ [▼ 目标物品多选列表]                                    │
│ [📋 应用到选中物品]                                     │
└─────────────────────────────────────────────────────────┘
```

#### 4.3.4 用户操作流程

```
1. 加载爆率文件（爆率修改页点击"加载爆率文件"）
2. （推荐）运行爆率模拟获取真实击杀/掉落数据
3. 切换到"爆率调配"标签页
4. 在物品下拉框中搜索并选择目标物品（如"裁决之杖"）
5. 点击"分析掉落来源"→ 显示该物品在哪些怪物/地图掉落
6. 检查数据来源提示（模拟数据优先级高于配置数据）
7. 设置每个地图的目标时间（如祖玛寺庙 15小时/个）
8. 选择取整模式（精准取整/阶梯取整/就近取整）
9. 点击"计算推荐爆率"→ 显示推荐修改表 + 偏差值
10. （可选）手动调整推荐爆率的分母值，期望与偏差实时更新
11. 点击"应用推荐值"→ 修改爆率文件
12. （可选）复制爆率到其他物品
```

#### 4.3.5 数据流与函数调用链

```
前端 (main.js)                         后端 (internal/app/app.go)
─────────────────                      ─────────────────────────
点击"分析掉落来源"
  │
  ├─► AnalyzeItemDrops(itemName)
  │     │
  │     ├─ simResult != nil ?
  │     │   ├─ YES → analyzeFromSim(itemName)
  │     │   │         遍历 simResult.ItemMonsterDrops[itemName]
  │     │   │         获取 killCnt(模拟击杀), simDropCnt(模拟掉落)
  │     │   │         kph = killCnt / durationH
  │     │   │         actualProb = simDropCnt / killCnt
  │     │   │         expectH = 1 / (kph * actualProb)
  │     │   │
  │     │   └─ NO  → 遍历 currentResults 配置文件
  │     │             kill_speed = efficiency / killTime
  │     │             spawn_speed = monsterCount / respawnSec
  │     │             real_kps = min(kill_speed, spawn_speed)
  │     │             kph = real_kps * 3600
  │     │             prob = entry.Probability()
  │     │             expectH = 1 / (kph * prob)
  │     │
  │     └─► 返回 RateAnalysis { sources[], totalKph, totalExpectH }
  │
  ├─► renderTuneSources(hasSim)  → 渲染掉落来源表格 + 数据来源提示
  ├─► renderTuneTargets()        → 按地图去重，生成目标时间输入卡片
  │
点击"计算推荐爆率"
  │
  ├─► RecommendRates(itemName, targets[], roundMode)
  │     │
  │     │  对每个掉落条目：
  │     ├─► calcExpectHours(monsterName, num, den)
  │     │     ├─ simResult != nil ? 用模拟数据 : 用配置+刷新模型
  │     │     └─ 返回 oldExpectH（当前期望小时）
  │     │
  │     ├─ rawDen = den * targetH / oldExpectH  // 核心反算公式
  │     ├─ newDen = roundDenominator(rawDen, roundMode)  // 按模式取整
  │     ├─ newExpectH = oldExpectH * newDen / den  // 反向校验
  │     ├─ deviation = (newExpectH - targetH) / targetH * 100  // 计算偏差
  │     │
  │     └─► 返回 RateChange[] { oldNum, oldDen, oldExpectH, newNum, newDen, newExpectH, deviation }
  │
  ├─► renderTuneRecommend()  → 渲染推荐表格（含偏差列）
  │     │
  │     └─ 每行 <tr> 带 data-old-exp 和 data-old-den
  │        分母 input 的 oninput 事件：
  │        newExpectH = oldExpectH * newDen / oldDen
  │        deviation = (newExpectH - targetH) / targetH * 100
  │
点击"应用推荐值"
  │
  ├─ 从表格读取用户修改后的 newDen
  ├─► ApplyRecommendedRates(itemName, changes[])
  │     遍历 changes → editor.ModifyEntry(file, entry, newNum, newDen)
  │
  └─► SaveAllModifiedFiles()  → 保存所有修改过的文件
```

#### 4.3.6 核心公式详解

##### 基础定义

- `num`：爆率分子（绝大多数场景为 1）
- `den`：爆率分母（传奇服务端使用整数分母）
- `p = num / den`：单次击杀掉落概率
- `kph`：每小时真实击杀数（只 / 小时），受刷新上限约束
- `expectH`：期望掉落间隔（小时 / 件）

##### 期望时间公式

```
expectH = den / (kph * num)
```

> 
> 核心结论：**在 kph 不变的前提下，期望时间与爆率分母成正比**。分母越大，爆率越低，期望间隔越长。

##### 反推推荐分母公式

已知当前：`den_old`、`expectH_old`
目标：`expectH_target`
同地图同玩家 kph 保持不变，推导得：

```
den_target_raw = den_old * (expectH_target / expectH_old)
```

> 
> 修正说明：原 v2.0.x 版本公式形式正确，但实现层存在分子分母颠倒的逻辑 bug，导致结果方向相反。本版本统一以分母为基准进行比例缩放，并增加单元测试覆盖。

##### 取整后反向校验

取整得到最终分母`den_new`后，必须重新计算真实期望：

```
expectH_new = expectH_old * (den_new / den_old)
deviation = (expectH_new - expectH_target) / expectH_target * 100%
```

偏差值正面展示给用户，正数代表实际间隔比目标长，负数代表比目标短。

#### 4.3.7 取整策略（三种模式可选）

表格

| 模式 | 规则 | 适用场景 | 示例（raw=3782） |
| --- | --- | --- | --- |
| 精准取整（推荐） | 向上取整到最近整数 | 追求最贴近目标，接受非整十整百分母 | 3782 → 3782（偏差≈0%） |
| 就近阶梯取整 | 取最接近的阶梯值，可上可下 | 希望分母规整，同时偏差最小 | 3782 → 4000（偏差≈6%） |
| 向上阶梯取整（兼容旧版） | 向上找到第一个≥raw 的阶梯值 | 严格保证爆率不高于目标，宁低不高 | 3782 → 5000（偏差≈32%） |

默认阶梯序列：
`1, 2, 5, 10, 20, 50, 100, 200, 300, 500, 1000, 2000, 3000, 4000, 5000, 10000, 20000, 50000...`

> 
> 优化说明：在原阶梯基础上增加 3000、4000 等中间档位，大幅降低取整偏差。超过 10 万后按万级阶梯。

#### 4.3.8 验证示例

##### 示例 1：方向错误修正验证

- 当前配置：爆率 `1/100`，模拟验证期望 `1小时/件`
- 用户目标：`5小时/件`
- 旧版错误结果：推荐 `1/50`（实际 0.2 小时 / 件，爆率反而更高）
- 新版正确计算：
  1. `den_target_raw = 100 * (5 / 1) = 500`
  2. 精准取整：`1/500`
  3. 反向校验：`expectH_new = 1 * (500 / 100) = 5小时`，偏差 0%

##### 示例 2：原文档场景优化对比

- 当前配置：爆率 `1/3000`，期望 `11.9小时/件`
- 用户目标：`15小时/件`
- 旧版结果：推荐 `1/5000`，实际 19.8 小时，偏差 + 32%
- 新版不同模式结果：

表格

| 模式 | 推荐爆率 | 修改后期望 | 偏差 |
| --- | --- | --- | --- |
| 精准取整 | 1/3782 | 15.0 小时 | +0% |
| 就近阶梯 | 1/4000 | 15.9 小时 | +6% |
| 向上阶梯 | 1/5000 | 19.8 小时 | +32%（兼容旧版） |

#### 4.3.9 常见问题排查指南

表格

| 现象 | 可能原因 | 排查方法 |
| --- | --- | --- |
| 推荐爆率方向反了（应变低却变高） | 实现层将分子分母比例搞反 | 单元测试验证：den=100, target=5h → 输出 den=500 |
| 分析结果期望时间不准 | 未运行模拟，使用了配置数据且未考虑刷新上限 | 检查状态栏是否显示 "配置文件 + MonGen 数据"，先运行模拟 |
| 推荐后期望时间与目标差距大 | 阶梯取整的精度限制 | 切换为 "精准取整" 模式查看理论最优值 |
| 分析和推荐的期望时间不一致 | 两者用了不同的 KPH 数据源 | 检查`RecommendRates`是否复用`calcExpectHours`函数 |
| 修改分母后期望不更新 | `oninput`事件未绑定 | 检查`renderTuneRecommend`末尾的事件绑定代码 |
| 表格列不对齐 | colgroup 宽度不一致或表格容器有滚动 | 检查两个表格的`<colgroup>`固定像素宽度是否一致 |
| 表格撑长页面 | 缺少 max-height | 检查`.tune-table-wrap`的`max-height: 320px` |
| 应用推荐值后文件没变 | `ApplyRecommendedRates`的 monsterIndex/entryIndex 错误 | 检查 RateChange 中的索引是否与 currentResults 对应 |
| 复制爆率到其他物品无效 | 目标物品在源怪物中不存在 | 检查`CopyRatesToItems`是否处理了新增条目的情况 |

### 4.4 授权管理模块

表格

| 功能 | 说明 | 状态 |
| --- | --- | --- |
| 机器码获取 | 采集 CPU / 主板 / 硬盘信息生成唯一机器码 | ✅ 已完成 |
| 激活码验证 | 验证激活码并绑定设备 | ✅ 已完成 |
| 授权状态检查 | 启动时自动校验授权 | ✅ 已完成 |
| 权限分级 | 试用 / 永久 / 季度授权 | 🔲 接口预留 |

---

## 5. 项目结构

```
legend-drop-tool/
├── main.go                        # Wails 入口
├── wails.json                     # Wails 项目配置
├── go.mod                         # Go 模块定义
├── internal/
│   └── app/
│       └── app.go                 # 后端绑定（所有 public 方法自动暴露给前端）
├── frontend/
│   └── dist/                      # 前端静态资源（//go:embed 嵌入二进制）
│       ├── index.html             # 主页面
│       ├── main.js                # 前端逻辑（调用 window.go.app.App.*）
│       └── style.css              # 样式（暗色主题）
├── pkg/
│   ├── parser/                    # 爆率文件解析
│   │   ├── types.go               # 数据类型定义
│   │   └── parser.go              # 解析引擎（支持全部格式）
│   ├── editor/                    # 爆率编辑
│   │   └── editor.go              # 编辑器（修改/新增/批量/重建）
│   ├── simulator/                 # 爆率模拟
│   │   └── simulator.go           # 模拟引擎（内置真实击杀速率模型）
│   ├── tuner/                     # 【新增】爆率调配计算
│   │   └── tuner.go               # 调配核心算法、取整策略、偏差计算
│   ├── backup/                    # 备份管理
│   │   └── backup.go              # 备份/恢复
│   ├── auth/                      # 授权管理
│   │   └── auth.go                # 机器码 + 激活码
│   └── config/                    # 配置管理
│       └── config.go              # 应用配置持久化
├── fonts/
│   └── fonts.go                   # 字体嵌入（预留）
├── .github/workflows/
│   └── build.yml                  # CI/CD 自动编译
├── README.md
└── 开发文档.md                    # 本文档
```

---

## 6. 开发进度

- 一期（基础完整版）— ✅ 已完成
  - 搭建 Go + Fyne 项目框架
  - 实现爆率文件解析引擎（HERO/GOM/GEE/BLUE）
  - 支持全部主流格式（传统 /#CHILD/#CALL/#CASE/#IF / 精准触发）
  - 实现爆率修改功能（单条 / 批量 / 倍率 / 新增 / 删除）
  - 实现爆率模拟功能（真实掉落逻辑）
  - 实现自动备份管理
  - 实现 GUI 界面（4 个标签页）
  - 配置 GitHub Actions 自动编译 exe
  - 解决中文乱码问题（CJK 主题）
  - 授权接口预留（骨架代码）
- 二期（Wails 迁移 + 功能增强）— ✅ 已完成
  - 框架迁移：Fyne v2 → Wails v2（WebView2 + Go 绑定）
  - 前端重构：原生 HTML/CSS/JS 暗色主题界面
  - 后端绑定：Go public 方法自动暴露为 `window.go.app.App.*`
  - 目录选择对话框：通过 `runtime.OpenDirectoryDialog` Go 绑定实现
  - 模拟筛选功能：指定物品 / 地图 / 怪物多选弹窗（带搜索、全选）
  - 筛选数据传递到模拟请求
- 三期（爆率调配重构优化 + UI增强）— ✅ 已完成
  - 拆解原调配模块计算逻辑，定位方向错误根因
  - 统一模拟与配置的击杀速率模型（刷新上限约束）
  - 重构反算公式，增加单元测试覆盖
  - 新增三种取整模式（精准 / 就近阶梯 / 向上阶梯）
  - 新增取整后反向校验与偏差百分比展示
  - 优化阶梯序列，增加中间档位降低偏差
  - 前端新增取整模式选择器、偏差列
  - 新增 tuner 独立包，与模拟、编辑模块解耦
  - 修复模拟爆率三表级联筛选 bug（selectedSimItem 状态管理）
  - 加载爆率文件和模拟时显示 loading 提示
  - 状态栏样式优化（金色文字、渐变背景）
  - **新增地图视图模式**：爆率修改页支持按地图→怪物→物品三列级联查看和编辑
  - **地图视图复制爆率**：右键怪物复制爆率，多选粘贴到其他怪物
  - **多地图怪物高亮**：出现在多个地图的怪物蓝色高亮，悬浮提示
  - **模拟地图名显示**：掉落地图列表关联 MapInfo.txt 显示中文名
- 四期（授权与增值）— 计划中
  - 实现云端授权后台
  - 权限分级管理
  - 软件自动更新功能

### 更新日志

#### v2.3.0（2026-09-19）

- 修复模拟爆率掉落地图列表只显示编号不显示名称 — SimMapStat 新增 DisplayName，关联 MapInfo.txt
- 新增 `GetMapDisplayNames` API，前端地图列表和级联筛选均显示中文名
- 爆率修改-地图视图新增复制爆率功能 — 右键怪物弹出菜单，支持复制/粘贴爆率到多选怪物
- 后端新增 `CopyMonsterRates(sourceIndex, targetIndices)` API
- 怪物列表新增 checkbox 多选 + 全选/全不选
- 地图视图多地图怪物高亮 — 蓝色背景+左边框，悬浮显示其他出现地图
- 预加载所有地图怪物数据缓存，避免逐次查询

#### v2.2.0（2026-09-19）

- 新增爆率修改「地图视图」模式 — 按地图维度查看怪物和掉落配置
- 三列级联布局：地图列表→怪物列表→掉落物品列表，与爆率模拟三个列表交互一致
- 点击地图显示该地图怪物，点击怪物显示掉落条目，点击物品弹出修改对话框
- 后端新增 `GetMapMonsterView()` / `GetMapMonsters(mapName)` API
- 地图名自动关联 MapInfo.txt 显示中文描述
- 三列均支持搜索过滤
- 工具栏新增「怪物列表/地图视图」切换标签

#### v2.1.3（2026-09-19）

- 修复模拟爆率掉落物品/地图/怪物三表级联筛选bug — 新增 selectedSimItem 状态，filterByMap 正确级联已选物品
- 支持再次点击已选中行取消选中，恢复上级数据
- 加载爆率文件和运行模拟时显示 loading 遮罩层（旋转动画+文字提示）
- 底部状态栏文字改为金色、背景渐变、字号增大加粗，更醒目

#### v2.1.2（2026-09-18）

- 修复爆率调配表格列与表头不对齐 — 移除 table-layout:fixed 和 colgroup 固定宽度，使用 width:100% 自适应
- 调配页面 section 标题和表头 sticky 悬浮 — 滚动时始终可见
- 修复调配页面内容溢出 — pane-tune 改为可滚动，复制爆率区域不再被截断
- 模拟参数新增「人物爆率(%)」— 默认 100% 即 1 倍爆率，上调/下调影响模拟掉落数值，后端通过 MapRateModifier 传入模拟器

#### v2.1.1（2026-09-18）

- 修复模拟结果中点击地图/物品后级联筛选失效 — filterByItem/filterByMap 正确传递 type 和 simData 给 renderCol
- 新增双击怪物名打开对应爆率文件 — 在掉落怪物列表中双击怪物名，自动切换到编辑 tab 并选中该怪物
- 使用 data-name 属性存储名称，避免级联后索引错位

#### v2.1.0（2026-09-18）

- 新增 pkg/tuner 独立调配计算包，封装取整策略和公式计算
- 修正推荐爆率方向错误（分子分母比例颠倒的 bug）
- 新增三种取整模式：精准取整/就近阶梯/向上阶梯（兼容旧版）
- 新增取整后反向校验与偏差百分比展示
- 优化阶梯序列，增加 3000/4000 等中间档位降低偏差
- RecommendRates API 新增 roundMode 参数
- RateChange 新增 deviation 字段
- 前端新增取整模式选择器和偏差列
- 新增 tuner 包单元测试覆盖方向/精度/取整场景

---

## 7. 编译与发布

### 本地编译

```
# 安装 Wails CLI
go install github.com/wailsapp/wails/v2/cmd/wails@latest

# 编译（Windows）
wails build -platform windows/amd64 -o legend-drop-tool.exe -ldflags "-s -w"
```

### 自动编译（GitHub Actions）

推送 tag 自动触发编译并发布 Release：

```
git tag v2.1.2
git push origin v2.1.2
```

编译产物：`legend-drop-tool.exe` （单文件，免安装，约 11MB）

---

## 8. 前后端通信规范

### 8.1 绑定调用约定

```
// 前端调用 Go 方法的标准写法
const result = await window.go.app.App.MethodName(arg1, arg2);
// 注意：
// - 命名空间是 window.go.app.App（不是 window.go.main.App）
// - Go 包名 package app 决定了 'app' 这一级
// - 所有 public 方法自动绑定
```

### 8.2 对话框调用

```
// Go 侧：通过 Wails runtime 打开系统对话框
import "github.com/wailsapp/wails/v2/pkg/runtime"

func (a *App) SelectDirectory() (string, error) {
    return runtime.OpenDirectoryDialog(a.ctx, runtime.OpenDialogOptions{
        Title: "选择目录",
    })
}
```

```
// 前端：调用 Go 绑定方法
const path = await window.go.app.App.SelectDirectory();
```

### 8.3 初始化时序

```
async function waitForWailsReady(maxWaitMs = 5000) {
  return new Promise((resolve, reject) => {
    if (window.go && window.go.app && window.go.app.App) return resolve();
    const start = Date.now();
    const timer = setInterval(() => {
      if (window.go && window.go.app && window.go.app.App) {
        clearInterval(timer);
        resolve();
      } else if (Date.now() - start > maxWaitMs) {
        clearInterval(timer);
        reject(new Error('Wails 绑定加载超时'));
      }
    }, 50);
  });
}

window.addEventListener('DOMContentLoaded', async () => {
  await waitForWailsReady();
  // 初始化应用...
});
```

### 8.4 爆率模拟 API

```javascript
// 运行模拟
const req = {
  durationHours: 24,        // 模拟时长(小时)
  killRatioPct: 50,          // 消灭比例(%)
  playerRatePct: 100,        // 人物爆率(%)，100%=1倍，200%=2倍，50%=半倍
  runCount: 1,               // 模拟轮数
  monsters: [],              // 指定怪物（空=全部）
  items: [],                 // 指定物品（空=全部）
  maps: []                   // 指定地图（空=全部）
};
const result = await window.go.app.App.RunSimulation(req);
// playerRatePct 通过 MapRateModifier 传入模拟器
// 直接作用于掉落概率计算：实际概率 = 原始概率 × (playerRatePct / 100)
```

### 8.5 爆率调配 API（修正版）

```
// 1. 分析物品掉落来源（优先使用模拟数据）
const analysis = await window.go.app.App.AnalyzeItemDrops('裁决之杖');
// 返回: { itemName, sources: [{ monsterName, monsterIndex, entryIndex, mapName, probStr, killPerHour, expectHours }], totalKph, totalExpectH }
// 说明: 如果已运行模拟，killPerHour基于实际模拟击杀数/时长，expectHours基于实际掉率
//       如果未模拟，使用 MonGen 刷新信息 + 爆率文件概率计算，并在状态栏提示

// 2. 计算推荐爆率
const targets = [
  { mapName: '祖玛寺庙', targetHours: 15 },
  { mapName: '赤月峡谷', targetHours: 20 }
];
const changes = await window.go.app.App.RecommendRates('裁决之杖', targets, 'precision');
// 第三个参数为取整模式：'precision'（精准）/'nearest'（就近阶梯）/'up'（向上阶梯）
// 返回: [{ monsterName, monsterIndex, entryIndex, mapName, oldNum, oldDen, oldExpectH, newNum, newDen, newExpectH, deviation }]

// 3. 应用推荐爆率（支持前端修改 newDen 后传入）
const count = await window.go.app.App.ApplyRecommendedRates('裁决之杖', changes);
// 前端修改 newDen 后，changes 中的 newDen 会被更新后的值覆盖

// 4. 复制爆率到其他物品
const result = await window.go.app.App.CopyRatesToItems('裁决之杖', ['骨玉权杖', '龙纹剑']);
// 返回: { success, modified, message }

// 5. 保存所有已修改文件
const saved = await window.go.app.App.SaveAllModifiedFiles();
```

### 8.6 前端实时更新逻辑

推荐表格中修改分母后，期望小时与偏差实时重算：

- 每行 tr 带有 `data-old-exp`（当前期望小时）和 `data-old-den`（当前分母）
- `oninput` 事件触发时：
```
newExpectH = oldExpectH * newDen / oldDen;
deviation = ((newExpectH - targetH) / targetH * 100).toFixed(1) + '%';
```
- 同步更新 `tuneRecommend` 数组数据，确保应用时使用修改后的值

### 8.7 地图视图 API（v2.2.0 新增）

```javascript
// 1. 获取所有地图及怪物数量
const maps = await window.go.app.App.GetMapMonsterView();
// 返回: [{ mapName: 'D001', displayName: 'D001 (银杏山谷)', monsterCount: 5 }, ...]
// 说明: 地图名来自 MonGen.txt，displayName 自动关联 MapInfo.txt 中文描述
//       若 MonGen.txt 无数据，从怪物掉落文件名推断

// 2. 获取指定地图下的怪物列表
const monsters = await window.go.app.App.GetMapMonsters('D001');
// 返回: [{ monsterName: '半兽人', monsterIndex: 3, entryCount: 8 }, ...]
// 说明: monsterIndex 为 currentResults 中的索引，可直接传给 GetEntries()
//       entryCount 为可编辑掉落条目数（排除注释/#CHILD/#CALL 等）

// 3. 获取怪物掉落条目（已有 API）
const entries = await window.go.app.App.GetEntries(monsterIndex);
// 返回: 该怪物的全部掉落条目（含嵌套结构）
```

#### 后端数据结构

```go
// MapViewMap 地图视图 - 地图信息
type MapViewMap struct {
    MapName      string `json:"mapName"`      // 地图编号（如 D001）
    DisplayName  string `json:"displayName"`  // 显示名称（如 D001 (银杏山谷)）
    MonsterCount int    `json:"monsterCount"` // 该地图怪物数量
}

// MapViewMonster 地图视图 - 怪物信息
type MapViewMonster struct {
    MonsterName  string `json:"monsterName"`  // 怪物名称
    MonsterIndex int    `json:"monsterIndex"` // currentResults 索引
    EntryCount   int    `json:"entryCount"`   // 可编辑掉落条目数
}
```

#### 前端调用流程

```
点击「地图视图」标签
  │
  ├─► loadMapView()\  │     ├─► GetMapMonsterView()  → 渲染地图列表（左列）
  │     └─ 清空怪物和条目列
  │
点击地图行
  │
  ├─► loadMapViewMonsters(mapName)
  │     ├─► GetMapMonsters(mapName)  → 渲染怪物列表（中列）
  │     └─ 清空条目列
  │
点击怪物行
  │
  ├─► loadMapViewEntries(monsterIndex)
  │     ├─► GetEntries(monsterIndex) → 渲染掉落条目（右列）
  │
点击可编辑条目
  │
  ├─► showMapViewEditDialog(monsterIndex, entryIndex, entry)
  │     └─ ModifyEntry() → 保存后刷新条目列表
```

#### 新增 API（v2.3.0）

```javascript
// 4. 复制怪物爆率到其他怪物
const count = await window.go.app.App.CopyMonsterRates(sourceIndex, targetIndices);
// sourceIndex: 源怪物在 currentResults 中的索引
// targetIndices: 目标怪物索引数组
// 返回: 成功复制的怪物数量
// 说明: 用源怪物的可编辑条目替换目标怪物的所有条目
//       需要手动调用 SaveAllModifiedFiles() 保存到文件

// 5. 获取地图显示名称映射
const mapNames = await window.go.app.App.GetMapDisplayNames();
// 返回: { 'D001': 'D001 (银杏山谷)', 'D002': 'D002 (比奇省)', ... }
// 说明: 供前端将地图编号转换为带中文描述的显示名

// 6. 地图统计中新增 displayName 字段
// RunSimulation 返回的 SimMapStat 现包含:
// { mapName: 'D001', displayName: 'D001 (银杏山谷)', dropCount: 123 }
```

#### 前端复制爆率流程

```
右键怪物行 → 弹出上下文菜单
  │
  ├─► 复制爆率: GetEntries(monsterIndex) → 缓存到 copiedMonsterRates
  │
  ├─► 粘贴爆率: 勾选 checkbox → CopyMonsterRates(src, targets)
  │     └─ 刷新怪物列表
  │
  ├─► 全选/全不选: 批量操作 checkbox
```

---

## 9. 已知问题与解决方案

表格

| 问题 | 原因 | 解决方案 |
| --- | --- | --- |
| `window.runtime.OpenDirectoryDialog is not a function` | Wails v2 前端 runtime 没有此方法 | 改用 Go 绑定 `window.go.app.App.SelectDirectory()` |
| `undefined: runtime.DialogOptions` | Wails v2.16.0 类型名为 `OpenDialogOptions` | 使用 `runtime.OpenDialogOptions` |
| `Cannot read properties of undefined (reading 'App')` | JS 命名空间错误 | Go 包名是 `app`，应用 `window.go.app.App.*` 而非 `window.go.main.App.*` |
| 爆率文件编码 | 部分文件为 ANSI 编码 | 解析器已支持 BOM 检测和自动处理 |
| 爆率调配推荐值方向错误 | 原实现分子分母比例颠倒 | 重构 tuner 包，新增单元测试覆盖正反场景 |
| 推荐值与目标偏差大 | 阶梯跨度过大 | 新增精准取整模式，优化阶梯序列，展示偏差值 |
| 配置计算期望与模拟结果差距大 | 配置数据未考虑怪物刷新上限 | 统一使用 real_kps = min (击杀速度，刷新速度) 模型 |
| 物品点击无反应 | 点击物品行未绑定事件 | 已修复：点击物品后自动筛选地图和怪物列表 |
| 地图点击无反应 | 点击地图行未绑定事件 | 已修复：点击地图后自动筛选该地图的怪物列表 |
| 模拟三表级联筛选失效 | filterByMap 未考虑当前选中的物品 | 已修复（v2.1.3）：新增 selectedSimItem 状态，filterByMap 级联已选物品 |
| 调配表格列不对齐 | 表格使用百分比宽度，不同表格列宽不一致 | 已修复：改用固定像素宽度 colgroup + width:max-content |

---

## 10. 单元测试要求（新增）

为避免计算逻辑回归错误，tuner 包必须覆盖以下测试用例：

1. 目标时间变长 → 分母变大 → 爆率变低
2. 目标时间变短 → 分母变小 → 爆率变高
3. 三种取整模式输出符合预期
4. 反向校验偏差计算正确
5. 多怪物多地图场景下，整体总期望与目标一致